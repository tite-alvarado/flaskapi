# Flask API

## Small API to crypt/decrypt

### RUN IT:
`docker-compose build && docker-compose up -d`

### TEAR DOWN:
`docker-compose down`

### TEST IT:
`sh tests.sh`

### SEE LOGS:
`docker logs -f flaskapp`
